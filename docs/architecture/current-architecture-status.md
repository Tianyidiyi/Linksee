# 架构文档定位说明

本目录（`docs/architecture`）用于记录系统架构设计与约束，例如：

1. 领域边界与模块职责
2. 服务间交互模式（HTTP/Socket/Worker）
3. 关键设计原则与技术决策
4. 非功能约束（可用性、扩展性、安全性）

交付进度、已实现接口清单、联调状态等“阶段性实现信息”，统一迁移至：

- `docs/status/current-delivery-status.md`

前后端联调接口约束，统一放在：

- `docs/api/前后端联调对齐说明.md`
