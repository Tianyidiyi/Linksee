【声明栏】时间 | 操作者 | 事件内容

2026-05-06 15:30 | zkx | 补充：Prisma 使用要点——以 schema.prisma 作为结构单一事实源；先执行 db push 或 migrate 再联调接口；生产优先 migrate 并保留回滚脚本；敏感信息只放环境变量并确保 DATABASE_URL 正确。

2026-05-06 15:36 | zkx | 规范：每次 push、合并、发起 PR 时，必须基于 git 实际变更写简洁更新说明，仅保留关键改动、影响范围与风险点。

2026-05-06 20:00 | zkx | feat(auth+users): Auth 模块全量实现，包含以下变更：
  [新增接口]
  - POST   /api/v1/auth/login                    登录，返回 AT+RT，forceChangePassword 标志
  - POST   /api/v1/auth/refresh                  滚动刷新 Access Token（RT 一次性消费）
  - POST   /api/v1/auth/logout                   登出，撤销 RT
  - POST   /api/v1/auth/change-password          自主改密（改后吊销所有旧 RT，返回新 AT+RT）
  - POST   /api/v1/auth/admin/reset-password     管理员/教师重置单用户密码
  - POST   /api/v1/auth/admin/batch-reset-password  教务批量重置密码（支持 userIds / 学生条件筛选）
  - GET    /api/v1/users/me                      获取当前用户 profile
  - PATCH  /api/v1/users/me                      更新个人 bio/location/email/avatarUrl
  - POST   /api/v1/users                         教务创建单个学生或教师账号
  - POST   /api/v1/users/batch/students          教务批量创建学生（逐条校验，成功/失败分开返回）
  - POST   /api/v1/users/batch/teachers          教务批量创建教师
  - PATCH  /api/v1/users/:id                     教务更新账号状态（isActive/realName）
  - POST   /api/v1/users/assistants              教师创建助教账号（同步写 teacher_assistants 归属）

  [数据库变更]
  - 新增 teacher_assistants 表：记录助教账号的归属老师（账号创建时写入，PK=assistantUserId 保证唯一归属）
  - 修改 assistant_bindings 表：去掉 teacher FK relation，teacherUserId 保留为审计字段
  - 原 assistant_bindings 的双重语义（归属+分配）已拆分为两张表，职责清晰
  参考：docs/api/auth/auth-tables-design-v2.md

  [基础设施]
  - JWT 中间件：requireAuth / optionalAuth / forceChangeGuard（首次登录门卫）
  - MinIO 默认灰色头像：defaultAvatarUrl 注入所有新建账号
  - 密码强度工具：generatePassword() / isStrongPassword()

  [测试]

  [文档]
  - 新增 docs/api/auth/users-api-v1.md          Users 模块 API 契约（所有接口的入参/响应/错误码）
  - 更新 docs/api/auth/auth-tables-design-v2.md  表清单从 5 张→6 张，拆表决策记录
  - 更新 docs/architecture/current-architecture-status.md  当前状态日期 + Auth 完成情况 + 风险列表
  - 更新 docs/api/README.md                      补充 users-api-v1.md 和 auth-tables-design-v2.md 链接

2026-05-07 | zkx | feat(courses)+fix(schema): Phase 1-4 表结构落库 + courses API 实现 + 类型修复
  [数据库变更]
  - 新增 Phase 1-4 共 11 张表（courses / course_teachers / course_members /
    assignments / assignment_group_configs / assignment_stages /
    groups / group_members / mini_tasks / chat_conversations / chat_messages）
  - 修正 assistant_bindings.courseId：从 VarChar(36) 改为 BigInt FK → courses.id
  - 以上变更通过 prisma db push 已同步至本地 collab 数据库

  [新增接口]
  - GET    /api/v1/courses                课程列表（按角色过滤：academic/teacher/assistant/student）
  - POST   /api/v1/courses                创建课程（academic only）
  - GET    /api/v1/courses/:id            课程详情（权限校验：课程成员/老师/助教/academic）
  - PATCH  /api/v1/courses/:id            更新课程名/描述/状态（academic only）
  - GET    /api/v1/courses/:id/teachers   列出课程老师
  - POST   /api/v1/courses/:id/teachers   指派老师（academic only）
  - DELETE /api/v1/courses/:id/teachers/:userId  移除老师（academic only）
  - GET    /api/v1/courses/:id/members    列出课程成员（支持 status 筛选）
  - POST   /api/v1/courses/:id/members/batch     批量导入学生（最大 500，upsert 语义）
  - DELETE /api/v1/courses/:id/members/:userId   移除成员（软删除 status=withdrawn）

  [修复]
  - apps/api/tsconfig.json：补充 "types": ["node"]
  - apps/api 本地安装 @types/node（根 workspace 未实际安装导致 node:crypto 类型缺失）
  - apps/api/src/infra/jwt-middleware.ts：req.headers.authorization 改为 req.header("authorization")
    （Express 推荐写法，兼容 header 名大小写不敏感）
  - src/courses/courses-router.ts GET /courses/:id：删除重复的 semester 字段声明

  [文档]
  - 新增 docs/architecture/socket-risks-and-errors.md
    · 3 个已知错误（Phase 4 代码写入前必须修复）：
      1. course.message.created 事件名三处缺失（contracts.ts / event-catalog.ts / event-handlers.ts）
      2. chat_conversations.created_by 原为 NOT NULL → 应改为可空（课程群为系统自动创建）
      3. chat_messages 缺 updated_at 列
    · 3 个风险（实现时主动规避）：
      风险2：HTTP 写库后 Socket 推送失败 → 客户端补拉兜底
      风险3：gateway.ts authenticate() 老师/助教应返回空 groupIds
      风险4：客户端内存 Set 去重刷新失效 → 改用消息 DB id 去重
  - 新增 docs/architecture/courses-api-review.md
    · Courses API v1 审查报告：10 个端点接口完整性审查（10/10）
    · 权限设置分析：9/10 正确，缺 lead 唯一性业务约束（设计文档要求）
    · 发现 4 处改进点：semester 重复（P0 已修）/ lead 唯一性检查（P1）/ 课程存在性检查（P1）
    · 提供修复代码片段与后续扩展建议
  - 更新 docs/api/courses/course-tables-design-v1.md：
    · chat_conversations.created_by 改为 VARCHAR(10) NULL
    · chat_messages 补充 updated_at DATETIME NOT NULL

2026-05-09 | codex | refactor(assignments+courses)+docs: 课程权限/材料存储抽离，Assignment/Stage 路由拆分，并同步文档到当前可运行接口
  [新增文件]
  - apps/api/src/courses/course-access.ts
  - apps/api/src/assignments/assignment-access.ts
  - apps/api/src/assignments/course-material-storage.ts
  - apps/api/src/assignments/assignment-stages-router.ts

  [代码结构调整]
  - assignments-router.ts 仅保留 Assignment 顶层 CRUD 与 assignment materials（上传/删除）
  - assignment-stages-router.ts 承接 Stage CRUD、状态流转与 stage materials（上传/删除）
  - auth/server.ts 挂载 assignmentStagesRouter
  - courses-router.ts 接入统一课程权限与课程教师关系查询辅助

  [后端开放接口（本次确认）]
  - GET/POST    /api/v1/courses/:courseId/assignments
  - GET/PATCH/DELETE /api/v1/assignments/:assignmentId
  - POST/DELETE /api/v1/assignments/:assignmentId/materials
  - GET/POST    /api/v1/assignments/:assignmentId/stages
  - GET/PATCH/DELETE /api/v1/stages/:stageId
  - POST/DELETE /api/v1/stages/:stageId/materials

  [文档]
  - 更新 docs/api/openapi/linksee-v1.yaml（Assignment/Stage 已实现接口、状态与文件元数据结构对齐）
  - 更新 apps/web/README.md（前端联调按已开放接口对齐，统一上传规范 multipart/form-data + files）
  - 更新 docs/architecture/current-architecture-status.md（改为“当前已实现范围 + 缺口 + 前后端对齐”）

2026-05-09 | codex | docs(refactor): 交付状态与架构定位分离，前后端联调文档独立归档
  [文档位置调整]
  - 新增 docs/status/current-delivery-status.md（承载阶段交付状态、已实现范围、缺口）
  - 新增 docs/api/前后端联调对齐说明.md（承载前后端联调对齐规则）
  - 调整 docs/architecture/current-architecture-status.md 为“架构文档定位说明”，仅保留架构目录边界声明
  - 调整 apps/web/README.md 为入口说明，链接到联调文档与交付状态文档

2026-05-10 | zkx | feat(collaboration): 群聊消息增强（编辑/撤回/回复/@/搜索/文件/ACK/会话）+ 文档与对接补齐
  [数据库变更]
  - chat_messages 增加 mentions/editedAt
  - 新增 chat_files（附件元数据、过期清理）
  - 新增 chat_conversation_reads（会话未读与已读位置）

  [新增接口]
  - PATCH/DELETE /api/v1/courses/:courseId/messages/:messageId（编辑/撤回）
  - PATCH/DELETE /api/v1/groups/:groupId/messages/:messageId（编辑/撤回）
  - GET /api/v1/courses/:courseId/messages/search
  - GET /api/v1/groups/:groupId/messages/search
  - POST /api/v1/courses/:courseId/announcements
  - POST /api/v1/groups/:groupId/announcements
  - POST /api/v1/chat/files/presign-upload
  - GET  /api/v1/chat/files/presign-download
  - GET  /api/v1/conversations
  - POST /api/v1/conversations/:conversationId/read
  - POST /api/v1/realtime/acks
  - GET  /api/v1/realtime/replay

  [事件/实时]
  - 新增 course.message.updated / course.message.deleted
  - 新增 group.message.updated / group.message.deleted
  - Socket 事件缓存 + ACK 去重 + 重放窗口

  [文件与清理]
  - chat-files 私有桶 + 30 分钟 presign
  - 单文件上限 500MB，常见类型白名单，保留 7 天
  - 清理脚本：apps/api/src/collaboration/chat-files-cleanup.ts

  [文档]
  - 新增 docs/api/collaboration/chat-messages-v1.md
  - 更新 docs/api/README.md / docs/collaboration/README.md / docs/api/前后端联调对齐说明.md / contracts

  [风险]
  - Prisma 变更已用 db push（无迁移历史），后续上线需补 migration
  - ACK/重放依赖 Redis，可用性需保证

2026-05-10 | zkx | test(collaboration) + docs(verification): 单元测试框架 + 前端联调与接口验收清单
  [测试框架]
  - 安装 jest 29.7.0 + ts-jest + supertest
  - 新增 jest.config.js 配置 ESM + TypeScript 支持
  - 新增 npm test / npm run test:watch / npm run test:coverage 脚本

  [单元测试]
  - 新增 src/collaboration/__tests__/chat-helpers.test.ts（19 个测试，87.75% 覆盖率）
    · 测试 parseLimit / parseCursorParam / resolveMessageType / normalizeMentions 等纯函数
    · 边界情况：缺失值、无效类型、上限截断、去重等
  - 新增 src/collaboration/__tests__/course-chat-router.integration.test.ts（集成测试框架，18 个场景）
    · 框架已建立，占位符就绪，待实现完整 HTTP + Socket 端到端测试

  [文档]
  - 新增 docs/api/collaboration/frontend-integration-checklist.md（前端完整自检清单）
    · 所有 8+ 接口的参数、响应、权限、错误处理
    · Socket 事件结构与消息推送预期
    · 性能指标与容限要求
    · 前端推荐库与 UI 交互建议
  - 新增 docs/api/collaboration/api-acceptance-checklist.md（后端接口验收清单）
    · 11 大接口群组的完整验收场景
    · 每个场景的输入、期望、实际结果对标
    · 权限边界、幂等性、Socket 事件验证
    · 性能与错误处理检查项
  - 新增 docs/api/collaboration/test-coverage-report.md（测试覆盖率报告）
    · 单元测试 37/37 通过，chat-helpers 87.75% 覆盖率
    · 集成测试框架就绪，后续迭代计划（短中长期）
    · 测试执行命令与覆盖率阈值配置说明

  [后续计划]
  - 短期：完善集成测试，实现真实 HTTP + Socket + DB 端到端验证
  - 中期：性能测试（分页、并发、文件上传）与错误场景测试
  - 长期：端到端自动化（Playwright/Cypress）+ CI/CD 集成

2026-05-12 | zkx | refactor(groups+minitasks+courses)+feat(phase5 schema)+docs: 组队/任务拆分与 Phase 5 结构补齐
  [后端实现]
  - groups 拆分为 group-details / group-members / group-requests / group-admin 四个路由
  - 新增 /api/v1/assignments/:assignmentId/groups/conversations 批量生成小组群会话
  - 新增小组详情与成员管理接口，支持移组、合并与归档
  - MiniTask API 落地 + 定时提醒任务（1d/1h/overdue）
  - 课程激活前置校验：需至少 1 位老师 + 助教绑定；激活时创建课程群

  [数据库/Schema]
  - MiniTask 增加 assigneeIds 与提醒字段，补索引
  - Phase 5 新增 submissions / submission_files / reviews 表与关联关系
  - Prisma generate 已成功执行

  [文档]
  - course-tables-design-v1.md 补齐 Phase 5 设计、会话创建时序、扩展项整理
  - collaboration README 增补课程群创建逻辑与审查权限建议
  - 前端状态与联调清单更新（Group/MiniTask 已实现）

  2026-05-12 | zkx | feat(submission+review): Phase 5 提交与评审接口落地
    [新增接口]
    - POST /api/v1/stages/:stageId/groups/:groupId/submissions  创建阶段提交（学生）
    - POST /api/v1/submissions/:submissionId/reviews            创建评审（老师/助教）
      - GET  /api/v1/stages/:stageId/groups/:groupId/submissions  获取阶段提交记录
      - GET  /api/v1/courses/:courseId/pending-reviews            获取待评审列表
    - PATCH /api/v1/reviews/:reviewId                            更新评审（作者）

  [后端实现]
  - 新增 submissions-router / reviews-router，接入权限校验、状态流转与参数校验
  - Review 创建时同步更新 submission.status，并推送 submission/review 事件
  - Submission 支持附件上传（MinIO），重交时事务内替换 submission_files

2026-05-12 | zkx | fix(review+courses validation)+feat(grading export/pipeline): 增量修复与收口
  [新增接口]
  - GET  /api/v1/courses/:courseId/pipeline-health           课程流水线健康检查（按阶段聚合未交/待评审/评分草稿/已发布）
  - GET  /api/v1/courses/:courseId/grades/export             成绩 CSV 导出
  - GET  /api/v1/courses/:courseId/reviews/export            评审 CSV 导出

  [接口增强]
  - POST /api/v1/courses/:courseId/grades/publish-batch      支持 blocked 明细返回与 strict 模式
  - GET  /api/v1/courses/:courseId/grade-drafts              课程评分草稿列表（分页筛选）
  - POST /api/v1/submissions/:submissionId/grade-drafts      增加前置约束：仅 approved/reviewed 可评分

  [修复]
  - 统一 review 事件语义：创建发 review.created，更新发 review.updated
  - 同步事件契约：shared/contracts 与 event-catalog 增加 review.updated
  - 修复课程成员列表 status 参数校验（active/withdrawn），非法入参返回 400
  - 修复课程列表筛选 status/academicYear/semester 参数校验，避免非法值进入查询
  - 修复 review 创建幂等分支：仅命中缓存复用，未命中但已存在评审返回冲突
  - 优化分组确认建群返回语义：created -> processedCount（幂等 upsert 语义更清晰）
  - 修复 admin 初始化缺失 profile 时的 realName 兜底校验，避免空名写入

  [文档]
  - docs/api/course-backend-endpoints-overview-v1.md 增加“老师确认分组后建群”醒目说明与新增评分/导出接口列表

