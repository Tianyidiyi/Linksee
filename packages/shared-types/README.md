# shared-types

此目录预留前后端共享类型定义。

## 建议内容

- Course / Assignment / Stage / Group / MiniTask 类型
- Submission / Review / Grade 类型
- API 请求与响应类型
- 角色、权限、状态枚举

## 约束

- 类型定义应与 `docs/api/contracts` 和 `docs/api/openapi` 保持一致。
- 不放具体业务流程实现。
